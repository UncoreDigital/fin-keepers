---
import HeroImage from "../../../assets/services/book-keeping/cfo.png";
import HeroImage_2 from "../../../assets/services/book-keeping/cfo_2.png";
---

<div class="bg-[#f5f7fa] pt-24 pb-16 sm:py-16 px-4 sm:px-6 lg:px-20 relative overflow-hidden">
  <div
    class="max-w-screen-xl mx-auto flex flex-col lg:flex-row items-start lg:items-center justify-between gap-6 sm:gap-8 lg:gap-24 relative"
  >
    {/* Left Content Area */}
    <div
      class="flex flex-col items-start text-left gap-6 lg:gap-8 max-w-2xl lg:w-1/2 order-2 lg:order-1"
    >
      <h1
        class="text-[32px] sm:text-[42px] lg:text-[52px] font-bold leading-tight text-[#151515]"
      >
        Temporary Solutions - Lasting Financial Impacts
      </h1>
      <p
        class="text-[16px] sm:text-[18px] leading-[27px] text-[#797979] max-w-xl"
      >
        Our financial leadership may be temporary, but the value it creates is
        lasting. At Finkeepers, our CFO services are designed to be short-term
        engagements that deliver long-term stability, smarter structures, and
        sustainable growth for your business.
      </p>
      <button
        id="contact-scroll-btn"
        class="flex w-[200px] sm:w-[280px] h-[44px] sm:h-[50px] px-[15px] justify-center items-center bg-[#ed7c00] rounded-[8px] cursor-pointer border border-transparent hover:bg-transparent hover:border-[#ed7c00] text-white hover:text-[#ed7c00] transition duration-300"
      >
        <span
          class="text-[14px] sm:text-[18px] font-semibold leading-[20px] sm:leading-[24px] whitespace-nowrap"
        >
          Book a Free Consultation
        </span>
      </button>
    </div>

    {/* Right Image and Overlapping Cards Area */}
    <div
      class="relative w-full lg:w-1/2 flex justify-center items-center h-[350px] sm:h-[450px] lg:h-[500px] mt-10 lg:mt-0 order-1 lg:order-2 group"
    >
      {/* Default Image */}
      <img
        src={HeroImage.src}
        alt="Business Woman with tablet and growth charts"
        class="w-full h-full object-contain object-center transition-opacity duration-700 ease-in-out opacity-100 group-hover:opacity-0 z-[50]"
      />
      <!-- Hover Image -->
      <img
        src={HeroImage_2.src}
        alt=""
        class="absolute inset-0 w-full h-full object-contain object-center transition-opacity duration-700 ease-in-out opacity-0 group-hover:opacity-100 z-[60]"
      />
    </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const contactBtn = document.getElementById('contact-scroll-btn');
    
    if (contactBtn) {
      contactBtn.addEventListener('click', function() {
        // First try to find contact-section
        let contactSection = document.getElementById('contact-section');
        
        // If not found, try alternative selectors
        if (!contactSection) {
          contactSection = document.querySelector('[id*="contact"]');
        }
        if (!contactSection) {
          contactSection = document.querySelector('.contact-section');
        }
        if (!contactSection) {
          contactSection = document.querySelector('footer');
        }
        
        if (contactSection) {
          const yOffset = -40; // 40px offset from the top
          const y = contactSection.getBoundingClientRect().top + window.pageYOffset + yOffset;
          window.scrollTo({ top: y, behavior: 'smooth' });
        } else {
          // Fallback: scroll to bottom of page
          window.scrollTo({ top: document.body.scrollHeight - window.innerHeight - 40, behavior: 'smooth' });
        }
      });
    }
  });
</script>
