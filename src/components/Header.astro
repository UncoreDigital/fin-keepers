<div
  class="main-container w-full px-[20px] md:px-[60px] py-[12px] bg-[#0e2726] flex justify-between items-center relative"
>
  <!-- Logo -->
  <div
    class="w-[121.549px] h-[66.001px] shrink-0 bg-[url(https://codia-f2c.s3.us-west-1.amazonaws.com/image/2025-07-26/wPNxHOs7Vh.png)] bg-no-repeat bg-[length:100%_100%]"
  >
  </div>

  <!-- Hamburger Button -->
  <button
    id="menu-toggle"
    class="md:hidden text-white"
    aria-label="Toggle menu"
  >
    <svg
      class="w-6 h-6"
      fill="none"
      stroke="currentColor"
      stroke-width="2"
      viewBox="0 0 24 24"
    >
      <path d="M4 6h16M4 12h16M4 18h16"></path>
    </svg>
  </button>

  <!-- Menu (Nav links) -->
  <div
    id="mobile-menu"
    class="absolute md:static top-full left-0 w-full md:w-auto bg-[#0e2726] md:flex transition-all duration-300 ease-in-out z-50 hidden"
  >
    <div
      class="flex flex-col md:flex-row gap-4 md:gap-[44px] px-5 py-4 md:p-0 items-center"
    >
      <!-- ...nav items like About, Services, etc... -->
      <span class="text-white">About</span>
      <span class="text-white">Services</span>
      <span class="text-white">Resources</span>
      <span class="text-white">Pricing</span>
      <span class="text-white">Contact Us</span>
    </div>
  </div>

  <!-- CTA Button -->
  <div
    class="hidden md:flex items-center gap-[8px] bg-[#ed7c00] px-[18px] py-[10px] rounded-full shadow-sm"
  >
    <span class="text-white font-semibold text-[16px]">Get In Touch</span>
    <img
      src="https://codia-f2c.s3.us-west-1.amazonaws.com/image/2025-07-26/tBO0khMhjy.png"
      class="w-[20px] h-[20px]"
    />
  </div>
</div>

<!-- ✅ Script -->
<script>
  const toggle = document.getElementById("menu-toggle");
  const menu = document.getElementById("mobile-menu");

  toggle?.addEventListener("click", () => {
    menu?.classList.toggle("hidden");
  });
</script>
