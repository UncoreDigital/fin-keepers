<!-- Hero Section Container -->
<div class="main-container mx-auto pt-24 py-12 px-4 sm:px-6 lg:px-20 bg-[#f5f7fa]">
  <div
    class="max-w-screen-xl mx-auto flex flex-col-reverse lg:flex-row items-center gap-12 lg:gap-24"
  >
    <!-- Left Content Area -->
    <div
      class="flex flex-col items-start text-left gap-6 max-w-2xl lg:w-1/2"
    >
      <!-- Main Headline -->
      <h1
        class="text-2xl sm:text-3xl lg:text-5xl font-extrabold leading-snug text-[#151515]"
      >
        Your Books. Our Solutions. <br class="hidden sm:inline" />Your Growth.
      </h1>

      <!-- Subheading -->
      <p
        class="text-sm sm:text-base lg:text-lg leading-relaxed text-[#797979] max-w-xl"
      >
        At Finkeepers, we help startups and SMBs simplify their U.S. journey with tailored financial solutions from bookkeeping and payroll to reporting, taxation, and CFO services, delivering clarity, compliance, and confidence for growth. 

      </p>

      <!-- Statistics Cards -->
      <div
        class="flex flex-col sm:flex-row items-start justify-start gap-4 sm:gap-6 mt-4 w-full"
      >
        <!-- Card 1 -->
        <div
          class="bg-white rounded-xl shadow-md p-5 flex flex-col items-start text-left w-full sm:w-auto"
        >
          <h3 class="text-xl sm:text-2xl font-extrabold text-[#ed7c00] mb-1">
            60+
          </h3>
          <p
            class="text-sm sm:text-base font-semibold text-[#797979] whitespace-nowrap"
          >
            Companies Trust Us
          </p>
        </div>

        <!-- Card 2 -->
        <div
          class="bg-white rounded-xl shadow-md p-5 flex flex-col items-start text-left w-full sm:w-auto"
        >
          <h3 class="text-xl sm:text-2xl font-extrabold text-[#ed7c00] mb-1">
            40%
          </h3>
          <p
            class="text-sm sm:text-base font-semibold text-[#797979] whitespace-nowrap"
          >
            Reduction in Turnaround Time
          </p>
        </div>

        <!-- Card 3 -->
        <div
          class="bg-white rounded-xl shadow-md p-5 flex flex-col items-start text-left w-full sm:w-auto"
        >
          <h3 class="text-xl sm:text-2xl font-extrabold text-[#ed7c00] mb-1">
            95%
          </h3>
          <p
            class="text-sm sm:text-base font-semibold text-[#797979] whitespace-nowrap"
          >
            Client Retention
          </p>
        </div>
      </div>
    </div>

    <!-- Right Image and Overlay -->
    <div
      class="relative w-full lg:w-1/2 flex justify-center lg:justify-end mt-12 lg:mt-0 group ml-16 lg:ml-0"
    >
      <!-- Main Image -->
      <img
        src="/img/home/sales.png"
        alt="Smiling woman in business suit"
        class="w-4/5 max-w-xs sm:max-w-sm lg:w-full lg:max-w-none lg:w-[600px] rounded-[30px] object-cover transition-opacity duration-700 ease-in-out opacity-100 group-hover:opacity-0"
      />
      <!-- Hover Image -->
      <img
        src="/img/home/sales_2.png"
        alt="Business Woman with tablet and growth charts - Hover"
        class="absolute top-0 left-0 w-4/5 max-w-xs sm:max-w-sm lg:w-full lg:max-w-none lg:w-[600px] rounded-[30px] object-cover transition-opacity duration-700 ease-in-out opacity-0 group-hover:opacity-100 z-[60]"
      />
      <div class="mr-20"></div>
      <!-- Overlay Card -->
      <!-- Small Overlay Card -->
      <!-- <img
        src="/img/about/chart-about.png"
        alt="Financial chart overlay"
        class="absolute top-[-16px] sm:top-6 right-3 sm:right-10 w-35 pr-2 sm:pr-0 sm:w-48 rounded-xl shadow-lg translate-x-[20%]"
      /> -->
    </div>
  </div>
</div>



<script>
  document.addEventListener('DOMContentLoaded', function() {
    const contactBtn = document.getElementById('contact-scroll-btn');
    
    if (contactBtn) {
      contactBtn.addEventListener('click', function() {
        // First try to find contact-section
        let contactSection = document.getElementById('contact-section');
        
        // If not found, try alternative selectors
        if (!contactSection) {
          contactSection = document.querySelector('[id*="contact"]');
        }
        if (!contactSection) {
          contactSection = document.querySelector('.contact-section');
        }
        if (!contactSection) {
          contactSection = document.querySelector('footer');
        }
        
        if (contactSection) {
          const yOffset = -40; // 40px offset from the top
          const y = contactSection.getBoundingClientRect().top + window.pageYOffset + yOffset;
          window.scrollTo({ top: y, behavior: 'smooth' });
        } else {
          // Fallback: scroll to bottom of page
          window.scrollTo({ top: document.body.scrollHeight - window.innerHeight - 40, behavior: 'smooth' });
        }
      });
    }
  });
</script>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const contactBtn = document.getElementById('contact-scroll-btn');
    
    if (contactBtn) {
      contactBtn.addEventListener('click', function() {
        // First try to find contact-section
        let contactSection = document.getElementById('contact-section');
        
        // If not found, try alternative selectors
        if (!contactSection) {
          contactSection = document.querySelector('[id*="contact"]');
        }
        if (!contactSection) {
          contactSection = document.querySelector('.contact-section');
        }
        if (!contactSection) {
          contactSection = document.querySelector('footer');
        }
        
        if (contactSection) {
          const yOffset = -80; // 40px offset from the top
          const y = contactSection.getBoundingClientRect().top + window.pageYOffset + yOffset;
          window.scrollTo({ top: y, behavior: 'smooth' });
        } else {
          // Fallback: scroll to bottom of page
          window.scrollTo({ top: document.body.scrollHeight - window.innerHeight - 40, behavior: 'smooth' });
        }
      });
    }
  });
</script>
